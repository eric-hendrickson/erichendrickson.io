/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {
        appDir: true,
    },
    output: 'export',
};

// eslint-disable-next-line no-undef
module.exports = nextConfig;
